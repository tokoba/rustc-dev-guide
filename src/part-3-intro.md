# ソースコード表現

このパートでは、ユーザーからの生のソースコードを取得し、コンパイラが簡単に扱える様々な形式に変換するプロセスについて説明します。これらは_中間表現（IR）_と呼ばれます。

このプロセスは、コンパイラがユーザーが要求したことを理解することから始まります：コマンドライン引数を解析し、何をコンパイルするかを決定します。その後、コンパイラはユーザー入力を一連のIRに変換します。これらのIRは、ユーザーが書いたものからますます遠ざかっていきます。
